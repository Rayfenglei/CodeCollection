<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv"
        android:layout_width="match_parent"
        android:layout_height="@dimen/rv_move_tv"
        android:background="@drawable/bg_selected_channel"
        android:layout_centerInParent="true"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:gravity="center"
        android:textSize="14sp"
        android:textColor="#212121"/>

    <FrameLayout
        android:id="@+id/img_edit"
        android:layout_width="@dimen/rv_move_fragment"
        android:layout_height="@dimen/rv_move_fragment">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@mipmap/ic_channel_edit"/>
    </FrameLayout>

</RelativeLayout>